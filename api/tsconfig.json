{
  "extends": "../config/tsconfig.base.json",

  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "types": ["node", "@fastify/jwt", "vitest/globals"],

    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },

  "include": [
    "src",
    "drizzle.config.ts",
    "jwt.d.ts",
    "test",
    "vitest.config.ts"
  ]
}
